POSTGRES_USER=golfops
POSTGRES_PASSWORD=replace-with-a-long-random-password
CLOUDFLARE_TUNNEL_TOKEN=replace-with-cloudflare-token

# Ollama — runs natively on Mac, reachable from Docker via host.docker.internal
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_EMBEDDING_MODEL=embeddinggemma:300m

# Gmail OAuth2 — create credentials at console.cloud.google.com
GOOGLE_CLIENT_ID=replace-with-google-client-id
GOOGLE_CLIENT_SECRET=replace-with-google-client-secret
GMAIL_CALLBACK_URL=https://golf.kjarisk.com/api/auth/gmail/callback

# Acuity Scheduling — booking source of truth. Get credentials at
# acuityscheduling.com → Integrations → API. Single-user → API key (Basic Auth), not OAuth2.
ACUITY_USER_ID=replace-with-acuity-numeric-user-id
ACUITY_API_KEY=replace-with-acuity-api-key
# Optional: how often (ms) to poll Acuity for new/changed bookings (default 5 min)
ACUITY_SYNC_INTERVAL_MS=300000
